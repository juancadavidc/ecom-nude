# Carrito — plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Que agregar un producto desde la ficha lo meta de verdad al carrito
(persistido en `localStorage`), y que el panel lateral del header (`BotonCarrito`)
liste esos items con la interacción completa del SPEC §4.4 — cantidad, precio,
eliminar, subtotal, "Finalizar compra".

**Architecture:** Lógica pura en `src/lib/carrito.ts` (mismo criterio de
separación que `producto-modelo.ts`/`filtros.ts`, testeable con Vitest sin
React). Un `CarritoProvider` (Context) envuelve `src/app/layout.tsx` — la raíz,
no solo `(tienda)/layout.tsx`, para que un futuro grupo de rutas `(checkout)`
también tenga acceso sin mover el provider — y persiste a `localStorage` en un
`useEffect`. `Variante.tsx` y `BotonCarrito.tsx` (ya son `'use client'`) se
conectan al contexto.

**Tech Stack:** Next.js 16 (App Router), React Context, `localStorage`, Vitest.

**Spec:** `docs/superpowers/specs/2026-09-10-carrito-design.md`

## Global Constraints

- `sku` es la llave de un item de carrito (ya único por variante en el schema
  de Postgres). No se inventa un segundo identificador.
- Sin validación de stock al agregar/actualizar cantidad — eso es del checkout
  (fuera de este plan).
- Sin Zustand ni otra librería de estado nueva — Context, como el resto del
  repo (`VarianteProvider`, `BuscadorProvider`).
- El botón "Finalizar compra" apunta a `/checkout`. Esa ruta no existe todavía
  (sub-proyecto siguiente) — un 404 ahí es el comportamiento esperado de este
  plan, no un bug a arreglar aquí.
- Ningún componente de servidor se toca: solo archivos que ya son o pasan a ser
  `'use client'`.
- Todos los comandos de este plan se corren desde `tienda/`, no desde la raíz
  del repo.

---

## Task 1: Lógica pura del carrito — `src/lib/carrito.ts`

**Files:**
- Create: `tienda/src/lib/carrito.ts`
- Test: `tienda/src/lib/carrito.test.ts`

**Interfaces:**
- Produces: `ItemCarrito`, `agregarItem`, `actualizarCantidad`, `eliminarItem`,
  `totalesDe` — consumidos por `CarritoProvider` (Task 2).

- [ ] **Step 1: Escribir el test que falla**

Crear `tienda/src/lib/carrito.test.ts`:

```ts
import { describe, expect, it } from 'vitest'
import { agregarItem, actualizarCantidad, eliminarItem, totalesDe, type ItemCarrito } from './carrito'

function item(sku: string, overrides: Partial<ItemCarrito> = {}): Omit<ItemCarrito, 'cantidad'> {
  return {
    sku,
    productoSlug: 'legging-duna',
    nombre: 'Legging Duna',
    color: 'Duna',
    talla: 'M',
    precio: 145000,
    imagen: '/fotos/legging-duna-duna-1.jpg',
    ...overrides,
  }
}

describe('agregarItem', () => {
  it('agrega un item nuevo con cantidad 1 por defecto', () => {
    const items = agregarItem([], item('LEG-DUN-M'))
    expect(items).toEqual([{ ...item('LEG-DUN-M'), cantidad: 1 }])
  })

  it('agrega con la cantidad que se le pase', () => {
    const items = agregarItem([], item('LEG-DUN-M'), 3)
    expect(items[0].cantidad).toBe(3)
  })

  it('si el sku ya existe, suma la cantidad en vez de duplicar la fila', () => {
    const primero = agregarItem([], item('LEG-DUN-M'), 1)
    const segundo = agregarItem(primero, item('LEG-DUN-M'), 2)
    expect(segundo).toHaveLength(1)
    expect(segundo[0].cantidad).toBe(3)
  })

  it('un sku distinto agrega una fila aparte', () => {
    const primero = agregarItem([], item('LEG-DUN-M'))
    const segundo = agregarItem(primero, item('LEG-DUN-S', { talla: 'S' }))
    expect(segundo).toHaveLength(2)
  })
})

describe('actualizarCantidad', () => {
  it('cambia la cantidad de la fila con ese sku', () => {
    const items = agregarItem([], item('LEG-DUN-M'), 1)
    const actualizado = actualizarCantidad(items, 'LEG-DUN-M', 5)
    expect(actualizado[0].cantidad).toBe(5)
  })

  it('a 0 o menos elimina la fila', () => {
    const items = agregarItem([], item('LEG-DUN-M'), 1)
    expect(actualizarCantidad(items, 'LEG-DUN-M', 0)).toEqual([])
    expect(actualizarCantidad(items, 'LEG-DUN-M', -1)).toEqual([])
  })
})

describe('eliminarItem', () => {
  it('quita la fila con ese sku', () => {
    const items = agregarItem([], item('LEG-DUN-M'))
    expect(eliminarItem(items, 'LEG-DUN-M')).toEqual([])
  })

  it('un sku que no existe no revienta ni cambia nada', () => {
    const items = agregarItem([], item('LEG-DUN-M'))
    expect(eliminarItem(items, 'NO-EXISTE')).toEqual(items)
  })
})

describe('totalesDe', () => {
  it('carrito vacio da ceros', () => {
    expect(totalesDe([])).toEqual({ cantidad: 0, subtotal: 0 })
  })

  it('suma cantidad y precio * cantidad de cada fila', () => {
    let items = agregarItem([], item('LEG-DUN-M', { precio: 145000 }), 2)
    items = agregarItem(items, item('TOP-BRI-S', { talla: 'S', precio: 89000 }), 1)
    expect(totalesDe(items)).toEqual({ cantidad: 3, subtotal: 379000 })
  })
})
```

- [ ] **Step 2: Correr el test y confirmar que falla**

```bash
cd tienda
npx vitest run src/lib/carrito.test.ts --no-coverage
```

Expected: FAIL — `Cannot find module './carrito'` (el archivo no existe todavía).

- [ ] **Step 3: Implementar `src/lib/carrito.ts`**

```ts
import type { Talla } from './producto-modelo'

/**
 * `sku` es la llave: ya es unico por variante en el schema de Postgres
 * (`variantes.sku`), asi que no hace falta inventar un segundo identificador
 * para el item de carrito.
 */
export type ItemCarrito = {
  sku: string
  productoSlug: string
  nombre: string
  color: string
  talla: Talla
  precio: number
  imagen: string
  cantidad: number
}

/** Si el sku ya esta en el carrito, suma la cantidad en vez de duplicar la fila. */
export function agregarItem(
  items: ItemCarrito[],
  nuevo: Omit<ItemCarrito, 'cantidad'>,
  cantidad = 1,
): ItemCarrito[] {
  const existente = items.find((i) => i.sku === nuevo.sku)
  if (!existente) return [...items, { ...nuevo, cantidad }]
  return items.map((i) => (i.sku === nuevo.sku ? { ...i, cantidad: i.cantidad + cantidad } : i))
}

/** Cantidad <= 0 elimina la fila en vez de dejar un item en 0. */
export function actualizarCantidad(items: ItemCarrito[], sku: string, cantidad: number): ItemCarrito[] {
  if (cantidad <= 0) return eliminarItem(items, sku)
  return items.map((i) => (i.sku === sku ? { ...i, cantidad } : i))
}

export function eliminarItem(items: ItemCarrito[], sku: string): ItemCarrito[] {
  return items.filter((i) => i.sku !== sku)
}

export function totalesDe(items: ItemCarrito[]): { cantidad: number; subtotal: number } {
  return items.reduce(
    (acc, i) => ({ cantidad: acc.cantidad + i.cantidad, subtotal: acc.subtotal + i.precio * i.cantidad }),
    { cantidad: 0, subtotal: 0 },
  )
}
```

- [ ] **Step 4: Correr el test y confirmar que pasa**

```bash
npx vitest run src/lib/carrito.test.ts --no-coverage
```

Expected: PASS — 9 tests.

- [ ] **Step 5: Commit**

```bash
git add src/lib/carrito.ts src/lib/carrito.test.ts
git commit -m "feat(tienda): logica pura del carrito (agregar/actualizar/eliminar/totales)"
```

---

## Task 2: `CarritoProvider` — estado + persistencia en `localStorage`

**Files:**
- Create: `tienda/src/components/carrito/CarritoProvider.tsx`
- Modify: `tienda/src/app/layout.tsx`

**Interfaces:**
- Consumes: `ItemCarrito`, `agregarItem`, `actualizarCantidad`, `eliminarItem`,
  `totalesDe` (Task 1).
- Produces: `useCarrito()` → `{ items, cantidadTotal, subtotal, agregar,
  actualizarCantidad, eliminar }` — consumido por `Variante.tsx` (Task 3) y
  `BotonCarrito.tsx` (Task 4).

- [ ] **Step 1: Crear `src/components/carrito/CarritoProvider.tsx`**

```tsx
'use client'

import { createContext, useContext, useEffect, useMemo, useState, type ReactNode } from 'react'
import {
  agregarItem,
  actualizarCantidad as actualizarCantidadDe,
  eliminarItem,
  totalesDe,
  type ItemCarrito,
} from '@/lib/carrito'

const CLAVE_STORAGE = 'nude-carrito'

type ContextoCarrito = {
  items: ItemCarrito[]
  cantidadTotal: number
  subtotal: number
  agregar: (item: Omit<ItemCarrito, 'cantidad'>, cantidad?: number) => void
  actualizarCantidad: (sku: string, cantidad: number) => void
  eliminar: (sku: string) => void
}

const Ctx = createContext<ContextoCarrito | null>(null)

/**
 * Arranca vacio tanto en servidor como en el primer render de cliente para no
 * desincronizar la hidratacion; un useEffect carga lo que haya en localStorage
 * justo despues de montar. Leer y escribir van en try/catch: localStorage
 * puede no existir, estar lleno o venir corrupto, y ninguno de esos casos debe
 * romper el carrito — solo deja de persistir.
 */
export function CarritoProvider({ children }: { children: ReactNode }) {
  const [items, setItems] = useState<ItemCarrito[]>([])

  useEffect(() => {
    try {
      const guardado = localStorage.getItem(CLAVE_STORAGE)
      if (guardado) setItems(JSON.parse(guardado))
    } catch {
      // localStorage no disponible o el JSON guardado esta corrupto: se sigue con carrito vacio.
    }
  }, [])

  useEffect(() => {
    try {
      localStorage.setItem(CLAVE_STORAGE, JSON.stringify(items))
    } catch {
      // localStorage lleno o bloqueado: el carrito sigue funcionando en memoria.
    }
  }, [items])

  const valor = useMemo<ContextoCarrito>(() => {
    const { cantidad, subtotal } = totalesDe(items)
    return {
      items,
      cantidadTotal: cantidad,
      subtotal,
      agregar: (item, cantidad = 1) => setItems((actual) => agregarItem(actual, item, cantidad)),
      actualizarCantidad: (sku, cantidad) => setItems((actual) => actualizarCantidadDe(actual, sku, cantidad)),
      eliminar: (sku) => setItems((actual) => eliminarItem(actual, sku)),
    }
  }, [items])

  return <Ctx.Provider value={valor}>{children}</Ctx.Provider>
}

export function useCarrito(): ContextoCarrito {
  const ctx = useContext(Ctx)
  if (!ctx) throw new Error('useCarrito necesita estar dentro de CarritoProvider')
  return ctx
}
```

- [ ] **Step 2: Envolver la raiz en `src/app/layout.tsx`**

En `tienda/src/app/layout.tsx`, agregar el import y envolver `{children}`:

```ts
import { CarritoProvider } from '@/components/carrito/CarritoProvider'
```

Reemplazar:

```tsx
      <body>{children}</body>
```

por:

```tsx
      <body>
        <CarritoProvider>{children}</CarritoProvider>
      </body>
```

- [ ] **Step 3: Verificar que el sitio sigue arrancando**

```bash
npm run dev
```

Expected: la home carga en `http://localhost:3000` sin errores en consola ni en
la terminal (Ctrl+C para parar cuando se confirme).

- [ ] **Step 4: Commit**

```bash
git add src/components/carrito/CarritoProvider.tsx src/app/layout.tsx
git commit -m "feat(tienda): CarritoProvider con persistencia en localStorage"
```

---

## Task 3: Conectar `Variante.tsx` — agregar de verdad al carrito

**Files:**
- Modify: `tienda/src/components/producto/Variante.tsx`

**Interfaces:**
- Consumes: `useCarrito()` (Task 2).

- [ ] **Step 1: Importar `useCarrito` y llamarlo dentro de `agregar()`**

En `tienda/src/components/producto/Variante.tsx`, agregar el import:

```ts
import { useCarrito } from '@/components/carrito/CarritoProvider'
```

Dentro de `VarianteProvider`, antes del `useMemo` que arma `valor`, agregar:

```ts
  const carrito = useCarrito()
```

Reemplazar la linea `agregar: () => setConfirmado(true),` dentro del objeto
`valor` por:

```ts
      agregar: () => {
        if (!talla || !variante) return
        carrito.agregar(
          {
            sku: variante.sku,
            productoSlug: producto.slug,
            nombre: producto.nombre,
            color,
            talla,
            precio: producto.precio,
            imagen: producto.imagenes[color][0],
          },
          1,
        )
        setConfirmado(true)
      },
```

Y agregar `carrito` al array de dependencias del `useMemo` (junto a `producto,
colores, tallas, color, talla, confirmado`).

`BarraCompra.tsx` ya solo llama a `agregar` cuando `listo` es `true`
(`talla && variante && variante.stock > 0`), asi que el `if (!talla ||
!variante) return` es una guarda defensiva, no una ruta que hoy se ejecute.

- [ ] **Step 2: Verificar tipos**

```bash
npx tsc --noEmit
```

Expected: sin errores.

- [ ] **Step 3: Commit**

```bash
git add src/components/producto/Variante.tsx
git commit -m "feat(tienda): agregar al carrito de verdad desde la ficha de producto"
```

---

## Task 4: Panel del carrito con items — `BotonCarrito.tsx`

**Files:**
- Modify: `tienda/src/components/layout/BotonCarrito.tsx`
- Modify: `tienda/src/lib/copy.ts`
- Modify: `tienda/src/app/globals.css`

**Interfaces:**
- Consumes: `useCarrito()` (Task 2), `formatCOP` (`src/lib/format.ts`),
  `Minus`/`Plus`/`Trash` (`src/components/ui/icons.ts`), `Boton`/`BotonLink`
  (`src/components/ui/Button.tsx`).

- [ ] **Step 1: Agregar la nota de envio a `src/lib/copy.ts`**

En `tienda/src/lib/copy.ts`, dentro de `microcopy`, agregar despues de
`finalizar: 'Finalizar compra',`:

```ts
  envioEnCheckout: 'El envio se calcula en el siguiente paso.',
  eliminarDelCarrito: 'Eliminar del carrito',
```

- [ ] **Step 2: Reescribir `src/components/layout/BotonCarrito.tsx`**

```tsx
'use client'

import { useState } from 'react'
import { useCarrito } from '@/components/carrito/CarritoProvider'
import { MarcaNude } from '@/components/brand/Logo'
import { Boton, BotonLink } from '@/components/ui/Button'
import { Panel } from '@/components/ui/Panel'
import { Handbag, Minus, Plus, Trash } from '@/components/ui/icons'
import { microcopy } from '@/lib/copy'
import { formatCOP } from '@/lib/format'
import type { ItemCarrito } from '@/lib/carrito'

/**
 * SPEC §4.4 — El carrito es un panel lateral que entra desde la derecha,
 * no una pagina aparte ni una barra inferior como la referencia.
 */
export function BotonCarrito() {
  const [abierto, setAbierto] = useState(false)
  const { items, cantidadTotal, subtotal, actualizarCantidad, eliminar } = useCarrito()

  return (
    <>
      <button
        type="button"
        className="icon-btn cart-btn"
        onClick={() => setAbierto(true)}
        aria-label={
          cantidadTotal === 0
            ? 'Carrito, vacio'
            : `Carrito, ${cantidadTotal} ${cantidadTotal === 1 ? 'producto' : 'productos'}`
        }
        aria-expanded={abierto}
      >
        <Handbag size={22} weight="light" />
        {cantidadTotal > 0 && (
          <span className="cart-count" aria-hidden="true">
            {cantidadTotal}
          </span>
        )}
      </button>

      <Panel
        abierto={abierto}
        onCerrar={() => setAbierto(false)}
        titulo="Tu carrito"
        pie={
          items.length > 0 ? (
            <div className="carrito-pie">
              <div className="carrito-subtotal">
                <span>Subtotal</span>
                <span>{formatCOP(subtotal)}</span>
              </div>
              <p className="carrito-nota-envio">{microcopy.envioEnCheckout}</p>
              <BotonLink href="/checkout" ancho onClick={() => setAbierto(false)}>
                {microcopy.finalizar}
              </BotonLink>
              <Boton variante="secundario" ancho onClick={() => setAbierto(false)}>
                {microcopy.seguirViendo}
              </Boton>
            </div>
          ) : undefined
        }
      >
        {items.length === 0 ? (
          <CarritoVacio onCerrar={() => setAbierto(false)} />
        ) : (
          <ul className="carrito-lista">
            {items.map((item) => (
              <FilaCarrito
                key={item.sku}
                item={item}
                onCantidad={(cantidad) => actualizarCantidad(item.sku, cantidad)}
                onEliminar={() => eliminar(item.sku)}
              />
            ))}
          </ul>
        )}
      </Panel>
    </>
  )
}

function FilaCarrito({
  item,
  onCantidad,
  onEliminar,
}: {
  item: ItemCarrito
  onCantidad: (cantidad: number) => void
  onEliminar: () => void
}) {
  return (
    <li className="carrito-item">
      <img src={item.imagen} alt="" className="carrito-item-img" />
      <div className="carrito-item-info">
        <p className="carrito-item-nombre">{item.nombre}</p>
        <p className="carrito-item-variante">
          {item.color} · {item.talla}
        </p>
        <div className="carrito-item-cantidad">
          <button
            type="button"
            className="icon-btn"
            onClick={() => onCantidad(item.cantidad - 1)}
            aria-label="Quitar una unidad"
          >
            <Minus size={16} weight="light" />
          </button>
          <span aria-live="polite">{item.cantidad}</span>
          <button
            type="button"
            className="icon-btn"
            onClick={() => onCantidad(item.cantidad + 1)}
            aria-label="Agregar una unidad"
          >
            <Plus size={16} weight="light" />
          </button>
        </div>
      </div>
      <p className="carrito-item-precio">{formatCOP(item.precio * item.cantidad)}</p>
      <button
        type="button"
        className="icon-btn"
        onClick={onEliminar}
        aria-label={`${microcopy.eliminarDelCarrito}: ${item.nombre}`}
      >
        <Trash size={18} weight="light" />
      </button>
    </li>
  )
}

/** SPEC §7 — "Todavia no has elegido nada." Nunca "Tu carrito esta vacio 😢". */
function CarritoVacio({ onCerrar }: { onCerrar: () => void }) {
  return (
    <div className="carrito-vacio">
      <MarcaNude alto={44} className="text-line" />
      <p className="quote">{microcopy.carritoVacio}</p>
      <BotonLink href="/leggings" variante="secundario" onClick={onCerrar}>
        Ver leggings
      </BotonLink>
    </div>
  )
}
```

`cantidad` deja de recibirse por prop: el header ya llama `<BotonCarrito />`
sin props (`src/components/layout/Header.tsx:78`), asi que no hay otro call
site que actualizar.

- [ ] **Step 3: Agregar CSS — `src/app/globals.css`**

Despues del bloque `.carrito-vacio` (seccion `10d. Carrito vacio`, antes de
`---------- 10e. Footer ----------`), agregar:

```css
/* ---------- 10d-2. Carrito con items (SPEC §4.4) ---------- */

.carrito-lista {
  display: flex;
  flex-direction: column;
  gap: 20px;
  margin: 0;
  padding: 0;
  list-style: none;
}

.carrito-item {
  display: grid;
  grid-template-columns: 64px 1fr auto;
  gap: 12px;
  align-items: start;
  padding-block: 4px;
  border-bottom: 1px solid var(--line-soft);
  padding-bottom: 20px;
}

.carrito-item-img {
  width: 64px;
  height: 85px;
  object-fit: cover;
  border-radius: var(--radius);
  background-color: var(--bg-alt);
}

.carrito-item-info {
  display: flex;
  flex-direction: column;
  gap: 6px;
}

.carrito-item-nombre {
  font-size: 13px;
  letter-spacing: 0.05em;
  text-transform: uppercase;
}

.carrito-item-variante {
  font-size: 13px;
  color: var(--text-muted);
}

.carrito-item-cantidad {
  display: flex;
  align-items: center;
  gap: 8px;
  font-variant-numeric: tabular-nums;
}

.carrito-item-precio {
  font-size: 14px;
  white-space: nowrap;
}

.carrito-pie {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.carrito-subtotal {
  display: flex;
  justify-content: space-between;
  font-size: 15px;
}

.carrito-nota-envio {
  font-size: 12px;
  color: var(--text-muted);
}
```

- [ ] **Step 4: Commit**

```bash
git add src/components/layout/BotonCarrito.tsx src/lib/copy.ts src/app/globals.css
git commit -m "feat(tienda): panel de carrito con items, cantidad, eliminar y subtotal"
```

---

## Task 5: Verificación manual end-to-end

**Files:** ninguno (solo verificación con el driver de `run-tienda`).

- [ ] **Step 1: Correr los tests**

```bash
cd tienda
npm test
```

Expected: todos los tests pasan, incluidos los 9 nuevos de `carrito.test.ts`.

- [ ] **Step 2: Lint**

```bash
npm run lint
```

Expected: limpio.

- [ ] **Step 3: Arrancar el dev server y agregar un producto**

```bash
npm run dev
```

Con el servidor arriba, en otra terminal:

```bash
node .claude/skills/run-tienda/driver.mjs eval /leggings/legging-duna \
  "document.querySelector('.size-btn:not(:disabled)')?.click()"
node .claude/skills/run-tienda/driver.mjs eval /leggings/legging-duna \
  "document.querySelector('.barra-compra button')?.click()"
node .claude/skills/run-tienda/driver.mjs eval /leggings/legging-duna \
  "document.querySelector('.cart-count')?.textContent"
```

Expected: el ultimo comando devuelve `"1"` — el contador del header ya refleja
el item agregado.

- [ ] **Step 4: Screenshot del panel con items**

```bash
node .claude/skills/run-tienda/driver.mjs eval /leggings/legging-duna \
  "document.querySelector('.cart-btn')?.click()"
node .claude/skills/run-tienda/driver.mjs shot /leggings/legging-duna carrito-con-item
```

**Mirar el screenshot**: debe verse la fila del producto (miniatura, nombre,
color/talla, cantidad, precio, boton eliminar) y el pie con subtotal, la nota
de envio y los dos botones ("Finalizar compra", "Seguir viendo").

- [ ] **Step 5: Confirmar que el carrito sobrevive a un refresh**

```bash
node .claude/skills/run-tienda/driver.mjs eval /leggings/legging-duna "location.reload()"
node .claude/skills/run-tienda/driver.mjs eval /leggings/legging-duna \
  "document.querySelector('.cart-count')?.textContent"
```

Expected: sigue devolviendo `"1"` — la persistencia en `localStorage` funciona.

- [ ] **Step 6: Detener el dev server**

Ctrl+C en la terminal donde corre `npm run dev`.
